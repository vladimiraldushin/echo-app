# 🚀 АВТОМАТИЧЕСКАЯ УСТАНОВКА FFMPEG!

## 🎯 Что добавлено:

**FFmpeg теперь устанавливается АВТОМАТИЧЕСКИ при первом открытии WebM файла!**

---

## 🔧 Как это работает:

### 1. Открываете WebM файл
```
🎬 WebM detected → auto-converting...
```

### 2. Приложение проверяет FFmpeg
```
❌ FFmpeg не найден!
📦 Автоматическая установка через Homebrew...
```

### 3. Автоматическая установка (1-2 минуты)
```
🍺 Homebrew найден: /opt/homebrew/bin/brew
📦 Устанавливаю FFmpeg... (это займёт ~1-2 минуты)
==> Downloading ffmpeg...
==> Installing ffmpeg...
✅ FFmpeg установлен!
```

### 4. Конвертация
```
🔄 Converting via ffmpeg (/opt/homebrew/bin/ffmpeg)...
✅ Converted to WAV! Processing...
```

---

## 📋 Процесс автоустановки:

```
WebM файл открыт
    ↓
Проверка FFmpeg
    ↓
Не найден?
    ↓
Проверка Homebrew
    ↓
Homebrew найден?
    ↓
Запуск: brew install ffmpeg
    ↓
Ожидание установки (~1-2 мин)
    ↓
✅ FFmpeg установлен!
    ↓
Конвертация WebM → WAV
    ↓
✅ Готово!
```

---

## ⚠️ Требования:

### Должен быть установлен **Homebrew**

Проверка:
```bash
which brew
# → /opt/homebrew/bin/brew (Apple Silicon)
# → /usr/local/bin/brew (Intel Mac)
```

Если Homebrew НЕ установлен:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

## 🎉 Преимущества:

### ✅ Никакой ручной работы
Пользователю НЕ нужно:
- Открывать Terminal
- Вводить команды
- Гуглить "как установить ffmpeg"

### ✅ Один раз и навсегда
FFmpeg устанавливается **один раз**, потом работает всегда.

### ✅ Прозрачность
Пользователь видит в консоли что происходит.

---

## ❌ Если Homebrew не установлен:

Выдаётся понятное сообщение:
```
❌ Homebrew не установлен!
📦 Установите Homebrew: https://brew.sh

❌ Не удалось установить FFmpeg автоматически!

WebM файлы требуют FFmpeg для конвертации.

Установите вручную:
1. Откройте Terminal
2. Выполните: brew install ffmpeg

Или установите Homebrew: https://brew.sh
```

---

## 🔥 Итого:

### Раньше:
1. Открыть WebM → ошибка
2. Читать инструкцию
3. Открыть Terminal
4. `brew install ffmpeg`
5. Перезапустить приложение
6. Открыть WebM снова

### Теперь:
1. Открыть WebM → **автоустановка FFmpeg**
2. **Готово!** 🎉

---

## 🚀 Первый запуск с WebM:

```
🎬 WebM detected → auto-converting...
❌ FFmpeg не найден!
📦 Автоматическая установка через Homebrew...
🍺 Homebrew найден: /opt/homebrew/bin/brew
📦 Устанавливаю FFmpeg... (это займёт ~1-2 минуты)

[Идёт установка... подождите немного]

✅ FFmpeg успешно установлен!
🔄 Converting via ffmpeg (/opt/homebrew/bin/ffmpeg)...
✅ Converted to WAV! Processing...
```

---

## 🎯 TL;DR:

**Просто откройте WebM файл — всё установится автоматически!** 🚀

**Никакой мудни, никаких команд в терминале!** 🔥
