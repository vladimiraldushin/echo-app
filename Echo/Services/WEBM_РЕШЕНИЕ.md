# ⚠️ WebM файл не открывается — решение

## 🔍 Проблема:

Ваш WebM файл (`Встреча в Телемосте 26.02.26 14-03-57 — запись.webm`) использует кодек, который AVFoundation (системный фреймворк macOS) не может прочитать.

**Ошибка:** `FFR_Common signalled err=-12847` = "Cannot Open"

---

## 💡 Причина:

WebM файлы могут использовать разные кодеки:
- ✅ **Поддерживаются**: Opus, Vorbis (аудио), VP8, VP9 (видео)
- ❌ **Не поддерживаются**: Некоторые экспериментальные кодеки или битые контейнеры

Ваш файл, вероятно, использует кодек который AVFoundation не распознаёт.

---

## ✅ Решения (3 варианта):

### 🎯 Вариант 1: QuickTime Player (быстрее всего)

1. Откройте файл в **QuickTime Player**
2. Меню → **Файл** → **Экспортировать как...** → **1080p** (или 720p)
3. Сохраните как `.mov` или `.mp4`
4. Откройте новый файл в Echo

**Плюсы:** Быстро, не нужно ничего устанавливать  
**Минусы:** Нужен QuickTime Player

---

### 🌐 Вариант 2: Онлайн конвертер (проще всего)

1. Откройте один из сайтов:
   - https://cloudconvert.com
   - https://convertio.co
   - https://online-convert.com

2. Загрузите ваш `.webm` файл
3. Выберите формат: **MP4** или **MP3** (только аудио)
4. Скачайте конвертированный файл
5. Откройте в Echo

**Плюсы:** Просто, работает везде  
**Минусы:** Нужен интернет, загрузка файла

---

### 💻 Вариант 3: ffmpeg через Terminal (для продвинутых)

#### Установка ffmpeg:
```bash
# Если у вас установлен Homebrew:
brew install ffmpeg

# Или скачайте с https://ffmpeg.org/download.html
```

#### Конвертация:
```bash
# В MP4 (видео + аудио):
ffmpeg -i "Встреча в Телемосте 26.02.26 14-03-57 — запись.webm" \
       -c:v h264 -c:a aac "output.mp4"

# Только аудио в MP3:
ffmpeg -i "Встреча в Телемосте 26.02.26 14-03-57 — запись.webm" \
       -vn -c:a libmp3lame -q:a 2 "output.mp3"

# Только аудио в M4A (лучшее качество):
ffmpeg -i "Встреча в Телемосте 26.02.26 14-03-57 — запись.webm" \
       -vn -c:a aac -b:a 192k "output.m4a"
```

**Плюсы:** Быстро, высокое качество, offline  
**Минусы:** Нужно установить ffmpeg

---

## 🎯 Рекомендация:

### Для вашего случая (файл из Телемоста):

**Лучший вариант: QuickTime Player**

1. Откройте файл в QuickTime Player (двойной клик)
2. Файл → Экспортировать как... → **1080p**
3. Сохраните как `output.mov`
4. Перетащите `output.mov` в Echo

**Займёт ~2-3 минуты**

---

## 📊 Альтернатива: VLC Media Player

Если QuickTime не открывает файл:

1. Установите VLC: https://www.videolan.org/vlc/
2. Откройте файл в VLC
3. Меню → **Медиа** → **Конвертировать/Сохранить**
4. Выберите файл → **Конвертировать**
5. Профиль: **Video - H.264 + MP3 (MP4)**
6. Нажмите **Начать**

---

## 🔬 Диагностика файла (опционально):

Если хотите узнать, какой кодек использует файл:

```bash
# Установите ffprobe (входит в ffmpeg):
brew install ffmpeg

# Проверьте кодек:
ffprobe "Встреча в Телемосте 26.02.26 14-03-57 — запись.webm"
```

Найдите строку с **Audio:** — там будет название кодека.

---

## 🎓 Почему так происходит:

WebM — это открытый контейнер, который может содержать разные кодеки. Apple AVFoundation поддерживает **только стандартные WebM кодеки** (Opus, Vorbis, VP8, VP9).

Если файл:
- Записан в старой версии программы
- Использует экспериментальный кодек
- Повреждён или неполный

...AVFoundation не сможет его открыть.

---

## ✅ После конвертации:

Откройте конвертированный файл в Echo — всё заработает! 🚀

**Поддерживаются 100%:**
- MP4 (H.264 + AAC)
- MP3
- M4A
- MOV (QuickTime)

---

## 💬 Нужна помощь?

Если не получается:
1. Проверьте, открывается ли файл в QuickTime Player или VLC
2. Попробуйте все 3 варианта конвертации
3. Если ничего не работает — файл может быть повреждён

---

**Извините за неудобства!** WebM — капризный формат 😅
