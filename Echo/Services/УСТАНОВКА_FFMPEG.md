# ✅ РЕШЕНИЕ: FFmpeg нужен для WebM!

## 🔥 Проблема:
**afconvert НЕ ПОДДЕРЖИВАЕТ WebM** — это формат Google, а Apple его не любит.

## ✅ Решение:
Установить **FFmpeg** — универсальный конвертер медиа.

---

## 📦 Установка FFmpeg (1 команда):

### Вариант 1: Homebrew (рекомендуется)
```bash
brew install ffmpeg
```

### Вариант 2: Скачать готовый бинарник
1. Открыть https://evermeet.cx/ffmpeg/
2. Скачать **ffmpeg** (Static Builds)
3. Разархивировать и переместить в `/usr/local/bin/`

```bash
# После скачивания:
sudo cp ~/Downloads/ffmpeg /usr/local/bin/
sudo chmod +x /usr/local/bin/ffmpeg
```

---

## ✅ Проверка установки:

```bash
which ffmpeg
# Должно вывести: /opt/homebrew/bin/ffmpeg или /usr/local/bin/ffmpeg

ffmpeg -version
# Должна показать версию
```

---

## 🚀 После установки:

1. **Перезапустите приложение**
2. Откройте ваш WebM файл
3. FFmpeg автоматически извлечёт аудио!

---

## 🎯 Что изменилось в коде:

### ❌ УДАЛЕНО:
- Попытка использовать `afconvert` для WebM (он его не поддерживает)

### ✅ ДОБАВЛЕНО:
- Прямое использование `ffmpeg` для WebM
- Проверка наличия ffmpeg по путям:
  - `/opt/homebrew/bin/ffmpeg` (Apple Silicon)
  - `/usr/local/bin/ffmpeg` (Intel Mac)
  - `/usr/bin/ffmpeg` (системный)
- Понятное сообщение об ошибке, если ffmpeg не найден

---

## 📋 Процесс обработки WebM:

```
WebM файл → Проверка расширения
    ↓
FFmpeg конвертация:
  ffmpeg -i input.webm -vn -ar 16000 -ac 1 -f wav output.wav
    ↓
Чтение WAV → Float32 samples
    ↓
✅ Готово!
```

---

## 🎉 Установите FFmpeg и всё заработает!

```bash
brew install ffmpeg
```

**5 минут — и навсегда забудете про проблемы с WebM!** 🚀
