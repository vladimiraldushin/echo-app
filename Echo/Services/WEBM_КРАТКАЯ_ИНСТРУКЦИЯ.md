# 🎯 WebM теперь работает через FFmpeg!

## ⚠️ ВАЖНО: afconvert НЕ поддерживает WebM!

**Apple не поддерживает формат WebM** (это формат Google).  
Поэтому для обработки WebM файлов **обязательно нужен FFmpeg**.

---

## 📦 Установка FFmpeg (1 команда):

```bash
brew install ffmpeg
```

### Альтернатива (без Homebrew):
1. Скачать: https://evermeet.cx/ffmpeg/
2. Установить:
```bash
sudo cp ~/Downloads/ffmpeg /usr/local/bin/
sudo chmod +x /usr/local/bin/ffmpeg
```

### Проверка:
```bash
which ffmpeg
# Должно вывести путь к ffmpeg
```

---

## ✅ Что исправлено в коде:

### ❌ УДАЛЕНО:
- Попытка использовать `afconvert` для WebM (он выдавал `typ?` — не распознаёт формат)

### ✅ ТЕПЕРЬ:
```swift
// WebM → сразу на FFmpeg (без попыток afconvert)
if url.pathExtension.lowercased() == "webm" {
    print("🎬 WebM detected → auto-converting...")
    return try await convertWithFFmpeg(url: url)
}
```

### FFmpeg конвертация:
```bash
ffmpeg -i input.webm -vn -ar 16000 -ac 1 -f wav output.wav
```
- `-vn` — только аудио (без видео)
- `-ar 16000` — частота 16kHz (требование FluidAudio)
- `-ac 1` — моно (1 канал)
- `-f wav` — формат WAV

---

## 🚀 После установки FFmpeg:

1. **Перезапустите приложение** (или просто пересоберите `⌘+B`)
2. Откройте ваш WebM файл
3. FFmpeg автоматически извлечёт аудио!

Вы увидите в консоли:
```
🎬 WebM detected → auto-converting...
🔄 Converting via ffmpeg (/opt/homebrew/bin/ffmpeg)...
✅ Converted to WAV! Processing...
```

---

## ❌ Если FFmpeg не установлен:

Вы увидите:
```
❌ FFmpeg не найден!
📦 Установите: brew install ffmpeg
```

И подробную ошибку:
```
❌ FFmpeg не установлен!

WebM файлы требуют FFmpeg для конвертации.

Установите его командой:
brew install ffmpeg

Или скачайте готовый бинарник с ffmpeg.org
```

---

## 🎯 Установите FFmpeg — и всё заработает!

```bash
brew install ffmpeg
```

**Без мудни — просто установите FFmpeg один раз!** 🚀

