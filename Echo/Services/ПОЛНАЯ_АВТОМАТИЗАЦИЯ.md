# 🔥 ПОЛНАЯ АВТОМАТИЗАЦИЯ: Homebrew + FFmpeg!

## 🚀 Что сделано:

### ✅ Автоустановка Homebrew
Если Homebrew не установлен — **устанавливается автоматически!**

### ✅ Автоустановка FFmpeg
Если FFmpeg не установлен — **устанавливается автоматически!**

### ✅ ПОЛНОСТЬЮ автоматический процесс
Пользователь **НЕ ДЕЛАЕТ НИЧЕГО** — даже Homebrew ставится сам!

---

## 📋 Процесс обработки WebM:

```
Открыт WebM файл
    ↓
Проверка FFmpeg
    ↓
FFmpeg найден?
    ├─ ДА → Конвертация ✅
    └─ НЕТ → Автоустановка FFmpeg
         ↓
    Проверка Homebrew
         ↓
    Homebrew найден?
      ├─ ДА → brew install ffmpeg
      │        ↓
      │      ✅ FFmpeg установлен → Конвертация
      │
      └─ НЕТ → Автоустановка Homebrew
               ↓
          Установка Homebrew (~3-5 мин)
               ↓
          ✅ Homebrew установлен
               ↓
          brew install ffmpeg (~1-2 мин)
               ↓
          ✅ FFmpeg установлен
               ↓
          Конвертация ✅
```

---

## 🎯 Пользовательский опыт:

### Сценарий 1: FFmpeg уже установлен
```
🎬 WebM detected → auto-converting...
🔄 Converting via ffmpeg (/opt/homebrew/bin/ffmpeg)...
✅ Converted to WAV! Processing...
```
**Время:** ~5-10 секунд

---

### Сценарий 2: Homebrew есть, FFmpeg нет
```
🎬 WebM detected → auto-converting...
❌ FFmpeg не найден!
📦 Автоматическая установка через Homebrew...
🍺 Homebrew найден: /opt/homebrew/bin/brew
📦 Устанавливаю FFmpeg... (это займёт ~1-2 минуты)

==> Downloading ffmpeg...
==> Installing ffmpeg...
✅ /opt/homebrew/Cellar/ffmpeg/7.0.1: 336 files, 77.1MB

✅ FFmpeg успешно установлен!
🔄 Converting via ffmpeg...
✅ Converted to WAV! Processing...
```
**Время:** ~1-2 минуты (только первый раз)

---

### Сценарий 3: НИ Homebrew, НИ FFmpeg нет (чистая система!)
```
🎬 WebM detected → auto-converting...
❌ FFmpeg не найден!
📦 Автоматическая установка через Homebrew...
❌ Homebrew не установлен!
📦 Автоматическая установка Homebrew...
🍺 Устанавливаю Homebrew... (это займёт ~3-5 минут)

==> Checking for `sudo` access (which may request your password)...
Password: [вводите пароль администратора]

==> Downloading and installing Homebrew...
==> Installation successful!
✅ Homebrew успешно установлен!

🍺 Homebrew найден: /opt/homebrew/bin/brew
📦 Устанавливаю FFmpeg... (это займёт ~1-2 минуты)

==> Downloading ffmpeg...
==> Installing ffmpeg...
✅ FFmpeg установлен!

🔄 Converting via ffmpeg...
✅ Converted to WAV! Processing...
```
**Время:** ~5-7 минут (только первый раз, потом мгновенно!)

---

## 🔧 Технические детали:

### 1. Автоустановка Homebrew
```swift
private func installHomebrewAutomatically() async throws {
    print("🍺 Устанавливаю Homebrew... (это займёт ~3-5 минут)")
    
    // Официальный скрипт установки
    let installScript = """
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    """
    
    let process = Process()
    process.executableURL = URL(fileURLWithPath: "/bin/bash")
    process.arguments = ["-c", installScript]
    
    try process.run()
    process.waitUntilExit()
    
    guard process.terminationStatus == 0 else {
        throw ConversionError.exportFailed("Homebrew installation failed")
    }
    
    print("✅ Homebrew установлен!")
}
```

### 2. Автоустановка FFmpeg (с автопроверкой Homebrew)
```swift
private func installFFmpegAutomatically() async throws {
    var brewPath = brewPaths.first(where: { 
        FileManager.default.fileExists(atPath: $0) 
    })
    
    // Если Homebrew не найден — устанавливаем!
    if brewPath == nil {
        try await installHomebrewAutomatically()
        brewPath = brewPaths.first(where: { 
            FileManager.default.fileExists(atPath: $0) 
        })
    }
    
    // Устанавливаем FFmpeg
    let process = Process()
    process.executableURL = URL(fileURLWithPath: brewPath!)
    process.arguments = ["install", "ffmpeg"]
    
    try process.run()
    process.waitUntilExit()
}
```

---

## ⚠️ Важно:

### Может потребоваться пароль администратора!
При установке Homebrew система **может запросить пароль**.

Это нормально — Homebrew требует права администратора для установки в `/usr/local` или `/opt/homebrew`.

---

## 🎉 Итого:

### Что нужно пользователю:
1. **Ничего!** Просто открыть WebM файл.
2. (Возможно) ввести пароль администратора при первом запуске.

### Что происходит автоматически:
1. Проверка FFmpeg
2. Проверка Homebrew
3. Автоустановка Homebrew (если нужно)
4. Автоустановка FFmpeg (если нужно)
5. Конвертация WebM
6. Обработка аудио

### Требования:
- **macOS** (очевидно 😄)
- **Пароль администратора** (для установки Homebrew, если его нет)

---

## 🚀 Запуск:

```bash
⌘ + B  # Пересобрать проект
```

**Откройте WebM файл:**
- Если всё установлено → мгновенная конвертация
- Если нет Homebrew → установка Homebrew (~3-5 мин) + FFmpeg (~1-2 мин)
- Если есть Homebrew, нет FFmpeg → установка FFmpeg (~1-2 мин)

**В следующие разы — мгновенно!** ⚡️

---

## 🔥 TL;DR:

**Просто откройте WebM файл — ВСЁ установится автоматически!**

- Homebrew? ✅ Установится сам
- FFmpeg? ✅ Установится сам
- Конвертация? ✅ Произойдёт сама

**АБСОЛЮТНО никакой ручной работы!** 🚀

---

## 🎯 Первый запуск на чистой системе:

1. Открыть WebM файл
2. Ввести пароль администратора (если попросит)
3. Подождать ~5-7 минут
4. **Готово!**

**Больше никогда не придётся ничего устанавливать вручную!** 🔥
