# 🔥 РЕШЕНИЕ: Установите FFmpeg!

## Проблема:
**afconvert не поддерживает WebM** (формат Google)

Ошибка `typ?` = "не знаю этот формат"

---

## ✅ Решение (1 команда):

```bash
brew install ffmpeg
```

---

## После установки:

```bash
⌘ + B  # Пересобрать
```

Откройте WebM файл → всё заработает! 🚀

---

## Проверка:

```bash
which ffmpeg
# → /opt/homebrew/bin/ffmpeg ✅
```

---

**Без FFmpeg WebM не заработает — установите его!** 🔥
